// variables6.rs
// Execute `rustlings hint variables6` or use the `hint` watch subcommand for a hint.

// Lesson: const variables must have type annotation and are immutable

const NUMBER: i32 = 3;
fn main() {
    println!("Number {}", NUMBER);
}
