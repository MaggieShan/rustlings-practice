// variables1.rs
// Make me compile!
// Execute `rustlings hint variables1` or use the `hint` watch subcommand for a hint.

// Lesson: let statement to declare local variables (rust can infer type)

fn main() {
    let x = 5;
    println!("x has the value {}", x);
}
